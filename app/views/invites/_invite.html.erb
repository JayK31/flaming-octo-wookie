  <% @invites.each do |invite| %>
    <% if invite.trip == @trip %>
        <%= link_to(invite.user.name, trip_invite_path(@trip, invite)) %><br/>
  <% end %>
<% end %>

<%= link_to(@trip_leader.name) %>
