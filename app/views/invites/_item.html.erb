<!--  this is in the show.html.erb, supposed to show the items a user is bringing on a trip -->

<% @items.each do |item| %>
    <% if item.user_id == @user.id %>
      <p><%= item.name %></p>
    <% end %>
  <% end %>
